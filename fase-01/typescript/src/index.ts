// Importando o servidor - express
import express from 'express';

// Importando a função 
import { helloWorld } from "./routes";

// Instancia o express
const app = express();

// Criação de uma rota
app.get('/', )


app.listen(3333);