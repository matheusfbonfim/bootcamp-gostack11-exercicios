// Componentes ? -> Importa React -> JSX
import React from 'react';

// Componente cabeçalho
function Header(){
  return (
    <header>
      <h1>ReactJS</h1>
    </header>
  );
}

// Exportando padrão
export default Header;